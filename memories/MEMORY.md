[2026-08-20] DSH Desktop 升级永久规则：用户优先追求桌面版快捷体验，升级 DeepSeek Harness 时必须做完整迁移而不是只替换版本号。升级前检查并保留 superproject、deepseek-harness 子模块和未提交本地改动；升级后对照上游 release 的 package manifest，处理删除/重命名包与新增 peer 依赖，重新生成并 dry-run 验证所有 Yarn patches，再更新 yarn.lock 和 submodule pin。特别要保护 Fast 开关：Fast 不是只存在源码 checkout，桌面实际运行的是 npm 包；必须确认安装后的 @deepseek-ai/dsh-client-ui-model-selection runtime 包含 Fast toggle、/fast 命令、fastPairOf/visibleModelGroups，并确认 @deepseek-ai/dsh-llm-pi-ai runtime 包含 provider-fast 自动路由和 priority serviceTier。每次升级后必须加/运行 package surface 回归断言，检查实际 node_modules runtime，不得只测源码。rc8 曾删除 dsh-client-schema-form、重命名 dsh-client-web-react 为 dsh-client-ui-renderer；rc8 新增运行时 peer 需显式加入 desktop deploy root。Yarn 4.18 默认 npmMinimalAgeGate=1440，刚发布版本可能被 quarantine，安装验证可临时用 YARN_NPM_MINIMAL_AGE_GATE=0，不要修改仓库配置。验证至少包括 build、typecheck、Desktop 全量测试、package surface、runtime closure、Fast/LLM 定向测试和 yarn install --immutable。不要因验证流程擅自关闭用户正在运行的桌面进程。
DSH的官网在https://github.com/deepseek-ai/deepseek-harness里，里面有DSH的开源代码。
§
[2026-08-20] 用户已同意将 DSH Desktop 升级记忆设计为可编辑、可在多次升级中自主积累和迭代；普通升级结果可自动沉淀，涉及永久规则的变更保留确认环节，避免错误经验污染长期规则。
